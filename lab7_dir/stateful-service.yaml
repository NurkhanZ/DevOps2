apiVersion: v1
kind: Service
metadata:
  name: headless-svc
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    name: web
---
apiVersion: v1
kind: Service
metadata:
  name: stateful-svc
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 8080
    targetPort: 80
