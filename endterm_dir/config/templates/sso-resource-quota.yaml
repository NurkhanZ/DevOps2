apiVersion: v1
kind: ResourceQuota
metadata:
  name: sso-resource-quota
  namespace: {{ .Values.global.namespace }}
spec:
  hard:
    requests.cpu: "1500m"
    limits.cpu: "4000m"
    requests.memory: "1.5Gi"
    limits.memory: "2.5Gi"
    pods: "15"
    persistentvolumeclaims: "5"

