apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres.fullname" . }}-service-headless
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
spec:
  ports:
  - name: {{ .Values.postgres.appName }}
    port: {{ .Values.postgres.port }}
  clusterIP: None
  selector:
    {{- include "postgres.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres.fullname" . }}-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
spec:
  ports:
  - name: {{ .Values.postgres.appName }}
    port: {{ .Values.postgres.port }}
  selector:
    {{- include "postgres.selectorLabels" . | nindent 4 }}
